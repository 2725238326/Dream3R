# Cycle 043 — W20 SOTA Feature Matrix expansion + v0.5 axes spec drafting

cycle_id: CYCLE-20260522-001

date: 2026-05-22

cycle_name: architecture-focus round after user re-prioritization; markdown-only; W20 SOTA matrix family-grouped second pass + v0.5 axes spec draft

## Goal

Convert the user re-prioritization 2026-05-22 — "开题报告和综述请你先放到一边，我们的架构是最重要的内容，当然后续平台更新也得放到日程里" — into two concrete architecture-track artifacts that do not require server / checkpoint / DEC authorization:

1. **A**: a family-grouped W20 SOTA Feature Matrix that maps every external 3R-relevant method named in `NEXT_PHASE_ROADMAP.md` (plus the 7 Composer expert adapters and several SPINE-required comparators) to the Dream3R module that absorbs the mechanism, with honest implementation-status labels matching the v0.4 closure state.
2. **B**: a v0.5 axes spec that names the 8 axes along which Dream3R should be promoted from v0.4 architecture closure to v0.5, each with explicit `closes_iff` criteria.

Constraints:

- Markdown only; no code change; v0.3 + v0.4 layers byte-identical.
- No checkpoint download authorization granted by this cycle.
- No server work authorized.
- 4DGS / GaussianHead stays out of the main forward by default.
- Proposal (Track C) and survey (Track B) work explicitly out of scope this cycle.

## User Direction

> 开题报告和综述请你先放到一边，我们的架构是最重要的内容，当然后续平台更新也得放到日程里。

Captured in memory at `C:\Users\27252\.claude\projects\e--Dream3R\memory\feedback_track_priorities.md` as a feedback memory: Track A architecture is primary; Track C 开题报告 and Track B 综述 are parked (both packaged at cycle 042 / cycle 037 state); platform / KYKT integration is "on the agenda" but scheduled later, not now.

Subsequent narrowing: user picked options **A + B** (W20 SOTA Feature Matrix + v0.5 axes spec) from a five-option menu (A SOTA matrix / B v0.5 spec / C v0.4 contract edge tests / D W22 visualization pack / E W26 STream3R relation). Sequencing: A first, then B (so the v0.5 axes spec can consume A's Gap Report).

## Sources Reviewed

Read in this cycle (relevant facts cross-link to the artifacts below):

- `ARCHITECTURE_V04_AGENT_PROMPT.md` (v0.4 closure authority, still active for v0.5 boundary policy)
- `ARCHITECTURE_V04_STATUS.md` (per-axis [x] / [~] / [ ] checklist; defines what is stub / fallback / proxy / contract-only entering v0.5)
- `code/dream3r/NEXT_PHASE_ROADMAP.md` (W19-W29 origin of W20)
- `code/dream3r/RECENT_PROGRESS.md` (Tier 1 / 2 / 3 evidence ledger; KITTI smoke pointmap L2 = 20.4747 = integration evidence, NSA branch weights compressed 0.3927 / selected 0.6073 / sliding 0.0)
- `code/dream3r/ATTENTION_RESEARCH_MATRIX.md` (narrower companion focused on attention / state mechanisms)
- `code/dream3r/composer_experts/method_profiles.py` (canonical 7-expert profile table — MASt3R / Fast3R / Spann3R / CUT3R / MoGe-2 / DepthAnything / Test3R with `implementation_status` field)
- `code/dream3r/SOTA_FEATURE_MATRIX.md` (existing 2026-05-10 first pass — superseded but preserved at the end of the new version)
- `registry/source_registry.md` (SRC-* IDs cited in family tables)
- `literature/INDEX.md` (literature board entry point and usage rules)
- `literature/SPINE_MEMORY.md` (required + advanced reading entries for Point3R / Mem3R / LONG3R / LoGeR / OVGGT / PAS3R / FILT3R / LongStream / tttLRM / CUT3R / STream3R)
- `planning/BRANCH_COMPARISON_MATRIX.md` (format reference for tables)

Not read this cycle (out of scope):

- `planning/proposal_dream3r/*` (Track C — parked)
- `3R-mix/*` (Track B — parked)
- any server-side artifact under `/hdd3/kykt26/code/dream3r/`

## Mechanisms Extracted

This was a synthesis cycle, not a source-mining cycle. No new mechanism rows added to `units/RESEARCH_UNIT_BANK.md`. The synthesis output is the family-to-module mapping captured in `code/dream3r/SOTA_FEATURE_MATRIX.md` Sections 1-9.

Notable cross-section observations (folded into the Gap Report):

1. **OnlineX is not a paper**, just a name in the W20 roadmap text. No `SRC-*` row exists. Flagged as a roadmap drafting artifact rather than silently included or silently dropped.
2. **AnchorSplat is similarly unresolved** — the W20 header reads "3DGS / AnchorSplat" but no project / paper is pinned. Flagged.
3. **MoGe-2** exists in `composer_experts/method_profiles.py` as one of the 7 registered adapters but has **no row** in `registry/source_registry.md` (arXiv 2507.02546). Inventory hygiene action: add an SRC row in a future cycle.
4. **NSA sliding branch weight = 0.000** on the KITTI smoke window pair. Either correct routing under short sequences, or evidence of a training-free bias. Surfaced as v0.5 axis A6.
5. **Test3R-as-an-off-path is not realized in code**: v0.4 `RepairExecutor` actions 1/2 use internal `model.forward` rerun, never `dispatch("test3r", images)`. Surfaced as v0.5 axis A5.
6. **VGGT gap is structural** not just a missing row — `capability_card` schema v2.1 lacks the feed-forward many-view sub-profile to fit VGGT distinctly from Fast3R. Surfaced as v0.5 axis A4 with `capability_card v2.2` requirement.
7. **Permanence dynamic_mask is proxy, not D2 final** — explicitly named `dynamic_mask_proxy` in v0.4 contracts. Surfaced as v0.5 axis A3 with explicit promotion criterion requirement.

## Research Units Created Or Updated

None this cycle.

## Scoring Changes

None this cycle. `units/IDEA_SCOREBOARD.md` and `units/REPRODUCTION_READINESS_MATRIX.md` unchanged.

## Decisions

No DECs proposed in this cycle. Both new artifacts (`SOTA_FEATURE_MATRIX.md` second pass, `SPEC-20260522-001-dream3r-v05-axes.md`) are candidate-not-final per DEC-20260501-004 — they are planning artifacts that propose axes and mappings, not approvals.

Each v0.5 axis (A1-A8) closure requires its own future DEC referencing the `closes_iff` clause in `SPEC-20260522-001`.

## Deferred Items

The following were noted during the cycle but **not addressed this cycle**:

1. Add a `SRC-2026-016 MoGe-2` row to `registry/source_registry.md` (inventory hygiene).
2. Resolve OnlineX / AnchorSplat ambiguity in `NEXT_PHASE_ROADMAP.md` W20 — either pin to specific papers or remove from the W20 method list.
3. Update `planning/BRANCH_COMPARISON_MATRIX.md` Score Matrix to reflect Composer's finalist status (the cycle 008.5 update note still says "deferred until cycle 009 case-card data lands"; data has since landed).
4. Add a `CRITICAL_NOTES.md` entry for the Julian Ost AAAI-2026 driving-permanence name-collision (per SRC-2026-010 row note).
5. W22 visualization pack (matplotlib + existing JSON; can be done locally without DEC).
6. W26 `STREAM3R_RELATION.md` design writeup (markdown-only; can be done locally without DEC).
7. v0.4 contract edge-case tests (None handling, larger batches, multi-tick state delivery).

None of these block cycle 043 closure.

## Next Action

Recommended next user action — listed in priority order under the 2026-05-22 priority (architecture > platform; proposal + survey parked):

**A. Review `SPEC-20260522-001-dream3r-v05-axes.md` and decide axis-closure sequencing.** The spec's "Axes summary and recommended sequencing" table groups A1-A8 into three sprints. The highest-leverage sprint-1 candidates (all requiring F-002 server authorization) are A1 DINOv3-S backbone, A2 per-adapter checkpoint loading, A4 VGGT adapter + capability_card v2.2, and A6 NSA sliding branch utility on longer KITTI. If the user wants to start axis closure, the first DEC should reference one of these and the `closes_iff` clause it satisfies.

**B. Or pick a markdown-only architecture follow-up** that does not require a DEC:

- W22 visualization pack (matplotlib over existing JSON artifacts)
- W26 STream3R relation writeup (pure design markdown)
- resolve OnlineX / AnchorSplat drafting artifacts in `NEXT_PHASE_ROADMAP.md`
- add MoGe-2 SRC row to `registry/source_registry.md`
- v0.4 contract edge-case tests (locally runnable; expands `tests/test_v04_*.py`)

**C. Or scope platform / KYKT integration** at the planning level (per user 2026-05-22 "后续平台更新也得放到日程里"). Scoping only; KYKT navigation + Codex frontend edits remain blocked per existing rules in `WORKFLOW_STATUS.md` "Blocked Until User Decision."

**D. Or pause and reassess.**

Track A architecture-first remains the mainline per DEC-20260506-001 and is now also confirmed primary by the 2026-05-22 user re-prioritization.

## Artifacts Produced This Cycle

| File | Status | Role |
|---|---|---|
| `code/dream3r/SOTA_FEATURE_MATRIX.md` | updated (supersedes 2026-05-10 first pass; preserves differentiation list + evidence map at end) | family-grouped W20 second pass: 8 family sections + cross-cutting status roll-up + gap report + update discipline |
| `specs/SPEC-20260522-001-dream3r-v05-axes.md` | new | v0.5 axes spec; 8 axes A1-A8; each with `closes_iff` + required actions + dependencies + non-promises + evidence label; candidate-not-final |
| `INDEX.md` | updated header timestamp + added `SOTA_FEATURE_MATRIX.md` + `NEXT_PHASE_ROADMAP.md` + `RECENT_PROGRESS.md` rows to `code/dream3r/` section + added `SPEC-20260522-001` row to `specs/` section | sync |
| `WORKFLOW_STATUS.md` | updated header + added two workstream rows (`Dream3R W20 SOTA Feature Matrix` + `Dream3R v0.5 axes spec`) + rewrote `Recommended Next User Decision` to lead with architecture per 2026-05-22 user priority | sync |
| `cycles/CYCLE-20260522-001.md` | new | this file |

Memory updated this cycle:

| File | Status | Role |
|---|---|---|
| `C:\Users\27252\.claude\projects\e--Dream3R\memory\MEMORY.md` | new | auto-memory index with track-priorities pointer |
| `C:\Users\27252\.claude\projects\e--Dream3R\memory\feedback_track_priorities.md` | new | feedback memory: architecture primary; proposal + survey parked; platform later |

## Honesty Boundary

- No claim is made that any v0.5 axis has been validated.
- No claim is made that the family-grouped SOTA matrix changes any v0.3 or v0.4 code behavior — it is documentation.
- The 5 deterministic-fallback Composer adapters (Spann3R / CUT3R / MoGe-2 / DepthAnything / Test3R) are honestly labeled as `deterministic fallback`, not as real models. The matrix's "Implementation Status Roll-Up" follows code (`method_profiles.py` + v0.4 `backend_status` reporting), not intent.
- The DINOv3-S backbone is honestly labeled as `stub`, matching `ARCHITECTURE_V04_STATUS.md` § 1.
- `OnlineX` and `AnchorSplat` are flagged as drafting artifacts in the W20 roadmap text rather than silently included with a fabricated mapping.
- The "Memory primitive comparators never benchmarked" statement is honest: Point3R / Mem3R / LONG3R / LoGeR / OVGGT / PAS3R / FILT3R / LongStream / SLAM3R rows all carry `comparator-only` status and zero head-to-head data.
- KITTI smoke pointmap L2 = 20.4747 is preserved as "integration evidence, not trained quality" per `RECENT_PROGRESS.md` Tier 2 framing.
