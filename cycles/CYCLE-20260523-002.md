# CYCLE-20260523-002: v0.5 documentation closeout and training monitor

cycle_id: CYCLE-20260523-002
date: 2026-05-23
scope: A4 + A5 verification + A8 + A6 supplemental evidence + bus-fix handoff
parent_spec: SPEC-20260522-001-dream3r-v05-axes.md
status: documentation-complete, training-running

---

## Objective

Convert the 2026-05-23 v0.5 implementation handoff into durable project records:

1. monitor the currently running server training job,
2. close out A6 bank-ablation evidence,
3. write A4 and A8 DEC records,
4. update the SOTA feature matrix,
5. record the bus-shape leak fix and A5 off-path Test3R wiring status.

## Training monitor (P0)

Server check at `2026-05-23 13:54:35 CST`:

- PID `3005990` is still alive: `python -m dream3r.train --preset small --batch_size 16 --epochs 20 --gpus 0`.
- Process state: running (`R`), about 100% CPU, about 4.3% host memory.
- GPU0 holds about 5481 MiB, sampled utilization was 0%; GPUs 1-3 were idle at 9 MiB.
- `/hdd3/kykt26/code/dream3r/runs/train_v05_bus_fix.log` exists but is still 0 bytes.
- The parent script `/tmp/tmpgt89vcwo` launches the training command without redirecting stdout/stderr to `train_v05_bus_fix.log`; the Python process stdout/stderr are pipes, not the log file.

Interpretation: the training job is still running, but the handoff tail command cannot monitor progress because the active process is not writing to `train_v05_bus_fix.log`. Continue monitoring with `ps`/`nvidia-smi` unless a new redirected run is launched.

### Replacement observable run

Because the original GPU0 run stayed unobservable, a replacement run was started on idle GPU1 without killing PID `3005990`.

- Config: `configs/train_v05_bus_fix_gpu1.yaml`
- Log: `runs/train_v05_bus_fix_gpu1.log`
- TensorBoard/log dir: `runs/train_v05_bus_fix_gpu1_tb`
- Checkpoints: `checkpoints_v05_bus_fix_gpu1`
- Command: `python -u -m dream3r.train --preset small --config configs/train_v05_bus_fix_gpu1.yaml --batch_size 16 --epochs 20 --gpus 1`
- Main PID observed: `3031594`
- Early progress at `2026-05-23 14:18 CST`: epoch 1 reached 4/31 batches; loss samples `8.977`, `9.350`, `9.265`, `9.422`.

Important caveat: this run is observable, but slow (~72-74s/batch in the first epoch). It should be monitored for whether this is acceptable or whether a smaller canary / profiler is needed.

## A6 supplemental evidence (P1)

`runs/kitti_bank_ablation.json` was complete (14KB). `dream3r/scripts/analyze_kitti_json.py` was extended to support both original per-window evaluator output and aggregated sweep/ablation output.

Server report:

| Config | Backend | Windows | NSA | Sliding fire | Branch mean (c/s/sl) | Bank first/last/max | Drift | Route regret | L2 |
|---|---|---:|---|---:|---|---|---:|---:|---:|
| cross_attn_10w_nsa | StateTokenRecurrence | 10 | yes | 100% | 0.000 / 0.656 / 0.344 | 40 / 256 / 256 | 0.671 | 0.0104 | 20.147 |
| cross_attn_30w_nsa | StateTokenRecurrence | 30 | yes | 100% | 0.086 / 0.636 / 0.278 | 40 / 256 / 256 | 0.746 | 0.0246 | 20.425 |
| mamba_10w_nsa | mamba_ssm | 10 | yes | 0% | 0.235 / 0.765 / 0.000 | 40 / 256 / 256 | 0.117 | 0.0117 | 20.295 |
| mamba_30w_nsa | mamba_ssm | 30 | yes | 100% | 0.163 / 0.791 / 0.046 | 40 / 256 / 256 | 0.128 | 0.0200 | 20.510 |
| cross_attn_30w_nonsa | StateTokenRecurrence | 30 | no | 0% | 1.000 / 0.000 / 0.000 | 40 / 256 / 256 | 0.282 | 0.0853 | 20.405 |
| mamba_60w_nsa | mamba_ssm | 52 | yes | 100% | 0.000 / 0.867 / 0.133 | 40 / 256 / 256 | 2.592 | 0.0067 | 20.450 |

Conclusion: 6/6 configs are `ok`; 4/5 NSA-enabled configs fire the sliding branch; non-NSA negative control keeps sliding at 0; all configs saturate the bank at K=256. This reinforces DEC-20260523-001: the 2-window zero was a short-horizon/low-bank routing effect, not a branch wiring failure.

## A4 closeout (P2)

Added `decisions/DEC-20260523-002-a4-vggt-capability-v22.md`.

Closed surface:

- `capability_card` v2.2 adds `feed_forward_manyview`.
- `vggt_adapter.py` exists and returns a fallback-safe `ExpertOutput`.
- `method_profiles["vggt"]` exists.
- `ExpertRegistry.register_all_defaults` registers 8 adapters.
- `ComposerDecision.backend_status` includes `vggt` and honestly reports fallback while no checkpoint is loaded.

Boundary: this DEC does not claim real VGGT inference. Checkpoint download and `backend == "real"` server validation remain a future checkpoint DEC.

## A5 verification status

Action 4 is wired as `test3r_offpath_verify`.

Evidence:

- `contracts.py` includes action 4 in `REPAIR_ACTION_NAMES`.
- `repair.py` includes `_test3r_offpath`.
- `tests/test_v04_test3r_offpath.py` covers 7 cases: primary output unchanged, report logging, fallback dispatch with images, stub recording without images, final `repair_action_log` key, action vocabulary, and direct Test3R fallback dispatch.

Boundary: action 4 is verification-only and does not replace the main reconstruction output.

## A8 closeout (P2)

Added `decisions/DEC-20260523-003-a8-memory-a1-action.md`.

Closed surface:

- `memory_action.py` defines six `MemoryA1Action` values.
- `StateUpdatePolicy` consumes drift, conflict, confidence, and window index.
- `TTTStateUpdater` is shape-safe scaffold code.
- `planning/TTT_PLAN.md` records W25 milestones.
- `tests/test_memory_action_policy.py` has 14 unit tests.

Boundary: real tttLRM-style gradient-step training and long-sequence server evidence are deferred to W25.

## A3 local start (P5)

A3 was started locally but **not closed**.

Implemented:

- `PermanenceOutput.dynamic_mask_final` added as an optional field alongside `dynamic_mask_proxy`.
- `ReconstructionOutput.dynamic_mask_final` added as an optional top-level field.
- `Permanence.forward` now emits `dynamic_mask_proxy`.
- CR-2 suppress-mask selection now prefers `dynamic_mask_final`, falls back to `dynamic_mask_proxy`, and only then falls back to legacy `suppress_static_write`.
- `V04Pipeline` carries the optional final mask through typed contracts and records `cr2_suppress_source` in the permanence log.

Boundary:

- No D2 promotion criterion or real-data validation was added.
- No A3 DEC was written; this is only the local contract/code start requested in the handoff.
- Current behavior falls back to `dynamic_mask_proxy` because no producer emits `dynamic_mask_final` yet.

## Bus shape-leak fix

The bus cross-batch shape leak fix from the handoff remains mandatory:

- `bus.py` provides `hard_reset()`.
- `model.py` calls `self.bus.hard_reset()` when `timestep == 0`.

Do not remove this branch. It prevents stale bus payloads from leaking across batches with incompatible shapes.

## SOTA matrix update (P3)

Updated `code/dream3r/SOTA_FEATURE_MATRIX.md`:

- VGGT moved from comparator-only to deterministic fallback adapter with `feed_forward_manyview`.
- tttLRM/A8 moved from comparator-only to scaffold at the Memory A1 action-policy level.
- NSA row now reflects DEC-20260523-001 instead of the stale "sliding branch unused" gap.
- Roll-up counts now reflect 8 Composer adapters and 5 deterministic fallback adapters.

## Test and evidence state

- Handoff reported local tests: 195 passed.
- Handoff reported server tests: 194 passed.
- This cycle verified the modified JSON analysis script with `python -m py_compile`.
- This cycle reran the updated analysis script on the server against `runs/kitti_bank_ablation.json`.
- This cycle reran local full tests before A3: `python -m pytest tests/ -q` -> 195 passed, 65 warnings, 151.41s.
- This cycle reran local full tests after A3 start: `python -m pytest tests/ -q` -> 196 passed, 65 warnings, 58.69s.

Server pytest was not rerun in this cycle because the only server-synced code change was the analysis script used above; the handoff's 194-pass server result remains the latest full server test record.

## Artifacts changed

- `dream3r/scripts/analyze_kitti_json.py`
- `cycles/CYCLE-20260523-001.md`
- `cycles/CYCLE-20260523-002.md`
- `decisions/DEC-20260523-002-a4-vggt-capability-v22.md`
- `decisions/DEC-20260523-003-a8-memory-a1-action.md`
- `paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md`
- `code/dream3r/SOTA_FEATURE_MATRIX.md`
- `code/dream3r/contracts.py`
- `code/dream3r/model.py`
- `code/dream3r/modules.py`
- `code/dream3r/orchestrator.py`
- `code/dream3r/tests/test_permanence_v2.py`
- `code/dream3r/tests/test_v04_architecture_contract.py`
